{
  "name": "quarry-dev",
  "description": "Local semantic search for Claude Code: /find, /ingest, /remember, /explain, /source, /quarry",
  "version": "1.10.1",
  "author": {
    "name": "Punt Labs",
    "email": "hello@punt-labs.com"
  },
  "mcpServers": {
    "quarry": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "if [ -f \"${HOME}/.punt-labs/mcp-proxy/quarry.toml\" ]; then exec mcp-proxy --config quarry; else exec quarry mcp; fi"
      ]
    }
  }
}
