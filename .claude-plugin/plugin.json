{
  "name": "quarry",
  "description": "Local semantic search for Claude Code: /find, /ingest, /remember, /explain, /source, /quarry",
  "version": "1.10.0",
  "author": {
    "name": "Punt Labs",
    "email": "hello@punt-labs.com"
  },
  "mcpServers": {
    "quarry": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "if command -v mcp-proxy >/dev/null 2>&1; then exec mcp-proxy ws://localhost:8420/mcp; else exec quarry mcp; fi"
      ]
    }
  }
}
