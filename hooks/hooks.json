{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "mcp__plugin_quarry(-dev)?_quarry__.*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/suppress-output.sh"
          }
        ]
      }
    ]
  }
}
